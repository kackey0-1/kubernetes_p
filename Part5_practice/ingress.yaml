apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: voting-app
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /$1
spec:
  rules:
  - http:
      paths:
      - path: /vote
        backend:
          serviceName: vote-ingress
          servicePort: 5000
      - path: /result
        backend:
          serviceName: result-ingress
          servicePort: 5001